# modelprint roadmap (next iterations, not now)

Noted 21 Aug 2026, launch night.

## Community probes to fold in
- @MancerAI_ : adversarial-string tokenizer set (50 strings, exact-match
  method), video-encoder budget check (matched GLM-5V-Turbo), API fingerprint
  of the 5.3 stack. Invited via reply on launch night.
- @sushsrinivasan : 11 tokenizer probes across 10 models (11/11 GLM match).
  Invited via reply on launch night.
- @BohuTANG : contrast-experiment method, 4 probe texts with per-text token
  deltas (the +75 constant-offset observation).

## Features
- Probe opt-in UI: let the user pick which probes run per lane (checkbox list
  from the registry) instead of always running all.
